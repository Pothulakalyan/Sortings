#include<bits/stdc++.h>
using namespace std;


void bubble_sort(int n,vector<int>&ve){
    for(int i=0;i<n-1;i++){
        for(int j=0;j<n-i-1;j++){
            if(ve[j]>ve[j+1]){
                swap(ve[j],ve[j+1]);

            }
        }
    }
}
int main(){
    int n;
    cin>>n;
    vector<int>ve(n);
    for(int i=0;i<n;i++){
        cin>>ve[i];
    }
    
    bubble_sort(n,ve);
    
    for(int i=0;i<n;i++) cout<<ve[i]<<" ";
}
